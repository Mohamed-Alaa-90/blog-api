info:
  name: create-post
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/posts/
  body:
    type: json
    data: |-
      {
        "title": "My First Post",
        "content": "Hello World"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
